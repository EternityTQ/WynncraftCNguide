import { a as TextTrack, T as TextTrackSymbol, l as loadScript, p as preconnect } from '../chunks/vidstack-52171b8a.js';
import { L as ListSymbol, I as IS_CHROME, i as isHLSSupported } from '../chunks/vidstack-1d3e68d6.js';
import { VideoProvider } from './vidstack-video.js';
import { p as peek, D as DOMEvent, l as listenEvent, e as effect, x as camelToKebabCase, y as isUndefined, k as isString, z as isFunction } from '../chunks/vidstack-fd2ab5eb.js';
import { Q as QualitySymbol, c as coerceToError } from '../chunks/vidstack-d51d30f7.js';
import { R as RAFLoop } from '../chunks/vidstack-1565cf64.js';

const toDOMEventType = (type) => camelToKebabCase(type);
class HLSController {
  constructor(_video) {
    this.R = null;
    this.T = null;
    this.d = {};
    this.e = /* @__PURE__ */ new Set();
    this.O = _video;
  }
  get instance() {
    return this.R;
  }
  setup(ctor, context) {
    this.S = context;
    const isLive = peek(context.$state.streamType).includes("live"), isLiveLowLatency = peek(context.$state.streamType).includes("ll-");
    this.R = new ctor({
      lowLatencyMode: isLiveLowLatency,
      backBufferLength: isLiveLowLatency ? 4 : isLive ? 8 : void 0,
      renderTextTracksNatively: false,
      ...this.d
    });
    const dispatcher = this.U.bind(this);
    for (const event of Object.values(ctor.Events))
      this.R.on(event, dispatcher);
    this.R.on(ctor.Events.ERROR, this.V.bind(this));
    for (const callback of this.e)
      callback(this.R);
    context.player.dispatch(new DOMEvent("hls-instance", { detail: this.R }));
    this.R.attachMedia(this.O);
    this.R.on(ctor.Events.AUDIO_TRACK_SWITCHED, this.W.bind(this));
    this.R.on(ctor.Events.LEVEL_SWITCHED, this.X.bind(this));
    this.R.on(ctor.Events.LEVEL_LOADED, this.Y.bind(this));
    this.R.on(ctor.Events.NON_NATIVE_TEXT_TRACKS_FOUND, this.Z.bind(this));
    this.R.on(ctor.Events.CUES_PARSED, this._.bind(this));
    context.qualities[QualitySymbol.$] = this.aa.bind(this);
    listenEvent(context.qualities, "change", this.ba.bind(this));
    listenEvent(context.audioTracks, "change", this.ca.bind(this));
    this.T = effect(this.da.bind(this));
  }
  da() {
    if (!this.S.$state.live())
      return;
    const raf = new RAFLoop(this.ea.bind(this));
    raf.ga();
    return raf.ha.bind(raf);
  }
  ea() {
    this.S.$state.liveSyncPosition.set(this.R?.liveSyncPosition ?? Infinity);
  }
  U(eventType, detail) {
    this.S.player?.dispatch(new DOMEvent(toDOMEventType(eventType), { detail }));
  }
  Z(eventType, data) {
    const event = new DOMEvent(eventType, { detail: data });
    let currentTrack = -1;
    for (let i = 0; i < data.tracks.length; i++) {
      const nonNativeTrack = data.tracks[i], init = nonNativeTrack.subtitleTrack ?? nonNativeTrack.closedCaptions, track = new TextTrack({
        id: `hls-${nonNativeTrack.kind}${i}`,
        src: init?.url,
        label: nonNativeTrack.label,
        language: init?.lang,
        kind: nonNativeTrack.kind
      });
      track[TextTrackSymbol.ia] = 2;
      track[TextTrackSymbol.ja] = () => {
        if (track.mode === "showing") {
          this.R.subtitleTrack = i;
          currentTrack = i;
        } else if (currentTrack === i) {
          this.R.subtitleTrack = -1;
          currentTrack = -1;
        }
      };
      if (nonNativeTrack.default)
        track.setMode("showing", event);
      this.S.textTracks.add(track, event);
    }
  }
  _(eventType, data) {
    const track = this.S.textTracks.getById(`hls-${data.track}`);
    if (!track)
      return;
    const event = new DOMEvent(eventType, { detail: data });
    for (const cue of data.cues) {
      cue.positionAlign = "auto";
      track.addCue(cue, event);
    }
  }
  W(eventType, data) {
    const track = this.S.audioTracks[data.id];
    if (track) {
      this.S.audioTracks[ListSymbol.fa](
        track,
        true,
        new DOMEvent(eventType, { detail: data })
      );
    }
  }
  X(eventType, data) {
    const quality = this.S.qualities[data.level];
    if (quality) {
      this.S.qualities[ListSymbol.fa](
        quality,
        true,
        new DOMEvent(eventType, { detail: data })
      );
    }
  }
  Y(eventType, data) {
    if (this.S.$state.canPlay())
      return;
    const { type, live, totalduration: duration, targetduration } = data.details;
    const event = new DOMEvent(eventType, { detail: data });
    this.S.delegate.f("stream-type-change", {
      detail: live ? type === "EVENT" && Number.isFinite(duration) && targetduration >= 10 ? "live:dvr" : "live" : "on-demand",
      trigger: event
    });
    this.S.delegate.f("duration-change", { detail: duration, trigger: event });
    const media = this.R.media;
    if (this.R.currentLevel === -1) {
      this.S.qualities[QualitySymbol.ka](true, event);
    }
    for (const track of this.R.audioTracks) {
      this.S.audioTracks[ListSymbol.r](
        {
          id: track.id + "",
          label: track.name,
          language: track.lang || "",
          kind: "main"
        },
        event
      );
    }
    for (const level of this.R.levels) {
      this.S.qualities[ListSymbol.r](
        {
          width: level.width,
          height: level.height,
          codec: level.codecSet,
          bitrate: level.bitrate
        },
        event
      );
    }
    media.dispatchEvent(new DOMEvent("canplay", { trigger: event }));
  }
  V(eventType, data) {
    if (data.fatal) {
      switch (data.type) {
        case "networkError":
          this.R?.startLoad();
          break;
        case "mediaError":
          this.R?.recoverMediaError();
          break;
        default:
          this.R?.destroy();
          this.R = null;
          break;
      }
    }
  }
  aa() {
    if (this.R)
      this.R.currentLevel = -1;
  }
  ba() {
    const { qualities } = this.S;
    if (!this.R || qualities.auto)
      return;
    this.R[qualities.switch + "Level"] = qualities.selectedIndex;
    if (IS_CHROME)
      this.O.currentTime = this.O.currentTime;
  }
  ca() {
    const { audioTracks } = this.S;
    if (this.R && this.R.audioTrack !== audioTracks.selectedIndex) {
      this.R.audioTrack = audioTracks.selectedIndex;
    }
  }
  i() {
    if (this.S)
      this.S.qualities[QualitySymbol.$] = void 0;
    this.R?.destroy();
    this.R = null;
    this.T?.();
    this.T = null;
  }
}

class HLSLibLoader {
  constructor(_lib, _ctx, _callback) {
    this.la = _lib;
    this.S = _ctx;
    this.qa = _callback;
    this.ma();
  }
  async ma() {
    const callbacks = {
      onLoadStart: this.na.bind(this),
      onLoaded: this.oa.bind(this),
      onLoadError: this.pa.bind(this)
    };
    let ctor = await loadHLSScript(this.la, callbacks);
    if (isUndefined(ctor) && !isString(this.la))
      ctor = await importHLS(this.la, callbacks);
    if (!ctor)
      return null;
    if (!ctor.isSupported()) {
      const message = "[vidstack]: `hls.js` is not supported in this environment";
      this.S.player.dispatch(new DOMEvent("hls-unsupported"));
      this.S.delegate.f("error", { detail: { message, code: 4 } });
      return null;
    }
    return ctor;
  }
  na() {
    this.S.player.dispatch(new DOMEvent("hls-lib-load-start"));
  }
  oa(ctor) {
    this.S.player.dispatch(
      new DOMEvent("hls-lib-loaded", {
        detail: ctor
      })
    );
    this.qa(ctor);
  }
  pa(e) {
    const error = coerceToError(e);
    this.S.player.dispatch(
      new DOMEvent("hls-lib-load-error", {
        detail: error
      })
    );
    this.S.delegate.f("error", {
      detail: { message: error.message, code: 4 }
    });
  }
}
async function importHLS(loader, callbacks = {}) {
  if (isUndefined(loader))
    return void 0;
  callbacks.onLoadStart?.();
  if (loader.prototype && loader.prototype !== Function) {
    callbacks.onLoaded?.(loader);
    return loader;
  }
  try {
    const ctor = (await loader())?.default;
    if (ctor && !!ctor.isSupported) {
      callbacks.onLoaded?.(ctor);
    } else {
      throw Error(
        false ? "[vidstack] failed importing `hls.js`. Dynamic import returned invalid constructor." : ""
      );
    }
    return ctor;
  } catch (err) {
    callbacks.onLoadError?.(err);
  }
  return void 0;
}
async function loadHLSScript(src, callbacks = {}) {
  if (!isString(src))
    return void 0;
  callbacks.onLoadStart?.();
  try {
    await loadScript(src);
    if (!isFunction(window.Hls)) {
      throw Error(
        false ? "[vidstack] failed loading `hls.js`. Could not find a valid `Hls` constructor on window" : ""
      );
    }
    const ctor = window.Hls;
    callbacks.onLoaded?.(ctor);
    return ctor;
  } catch (err) {
    callbacks.onLoadError?.(err);
  }
  return void 0;
}

const JS_DELIVR_CDN = "https://cdn.jsdelivr.net";
class HLSProvider extends VideoProvider {
  constructor() {
    super(...arguments);
    this.$$PROVIDER_TYPE = "HLS";
    this.c = null;
    this.a = new HLSController(this.video);
    this.b = `${JS_DELIVR_CDN}/npm/hls.js@^1.0.0/dist/hls${".min.js"}`;
  }
  /**
   * The `hls.js` constructor.
   */
  get ctor() {
    return this.c;
  }
  /**
   * The current `hls.js` instance.
   */
  get instance() {
    return this.a.instance;
  }
  static {
    /**
     * Whether `hls.js` is supported in this environment.
     */
    this.supported = isHLSSupported();
  }
  get type() {
    return "hls";
  }
  get canLiveSync() {
    return true;
  }
  /**
   * The `hls.js` configuration object.
   *
   * @see {@link https://github.com/video-dev/hls.js/blob/master/docs/API.md#fine-tuning}
   */
  get config() {
    return this.a.d;
  }
  set config(config) {
    this.a.d = config;
  }
  /**
   * The `hls.js` constructor (supports dynamic imports) or a URL of where it can be found.
   *
   * @defaultValue `https://cdn.jsdelivr.net/npm/hls.js@^1.0.0/dist/hls.min.js`
   */
  get library() {
    return this.b;
  }
  set library(library) {
    this.b = library;
  }
  preconnect() {
    if (!isString(this.b))
      return;
    preconnect(this.b);
  }
  setup(context) {
    super.setup(context);
    new HLSLibLoader(this.b, context, (ctor) => {
      this.c = ctor;
      this.a.setup(ctor, context);
      context.delegate.f("provider-setup", { detail: this });
      const src = peek(context.$state.source);
      if (src)
        this.loadSource(src);
    });
  }
  async loadSource(src, preload) {
    if (!isString(src.src))
      return;
    this.g.preload = preload || "";
    this.a.instance?.loadSource(src.src);
    this.h = src;
  }
  /**
   * The given callback is invoked when a new `hls.js` instance is created and right before it's
   * attached to media.
   */
  onInstance(callback) {
    const instance = this.a.instance;
    if (instance)
      callback(instance);
    this.a.e.add(callback);
    return () => this.a.e.delete(callback);
  }
  destroy() {
    this.a.i();
  }
}

export { HLSProvider };
